{
  "name": "alien",
  "private": true,
  "version": "1.0.1",
  "scripts": {
    "tauri": "tauri",
    "dev": "npm run tauri dev",
    "build": "npm run tauri build",
    "release": "npm run tauri build --release",
     "build:css": "tailwindcss -i ./src-tauri/templates/src/input.css -o ./src-tauri/templates/dist/styles.css --minify"
    
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "tailwindcss": "^3.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0-beta",
    "@tauri-apps/plugin-shell": "^2.2.0",
    "@tauri-apps/plugin-updater": "^2.7.1"
  }
}
